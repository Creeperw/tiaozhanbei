---
skill_id: expert.generate_paper_blueprint
version: 1.1.0
agent: expert_agent
task_type: paper_blueprint
---

# 任务目标

在检索题目之前，根据用户组卷诉求和考试约束生成结构化试卷蓝图。每个蓝图单元必须能直接驱动后续题目检索。

## 蓝图单元要求

每个单元写明：知识模块、学习目标、检索表达、可选题型偏好、目标题数、可选分值、候选数量、选题规则、结构化考查维度和排除维度。

- `assessment_dimensions` 与 `excluded_dimensions` 只能从输出合同的固定枚举中选择；前者表示本单元允许考查什么，后者表示明确不能考查什么。不要自行创造枚举值。例如“只考四君子汤组成，不涉及类方对比和加减方”应分别写为 `composition`，以及 `comparison_differentiation`、`modification_extension`。

- 蓝图开头必须分别用单独一行明确写出“试卷标题”和“范围摘要”；
  “范围摘要”应完整说明本卷覆盖边界，供内部 compiler 原样提取，不能只把范围
  分散写在适用对象、短期计划或多个段落里。
- 每个单元的“知识模块”“学习目标”“检索表达”都写成单行原文；
  不要把一个字段拆成多级列表，避免内部 compiler 为拼接字段而改写原文。
- `question_type_preferences` 允许为空；题型缺失时后续检索仍应执行。
- 用户明确要求“选择题专项”时，每个蓝图单元的 `question_type_preferences` 只能使用 `单项选择题`、`多项选择题`，不得加入判断题、填空题、简答题、问答题或病例问答。用户只说“选择题”但未限定单选/多选时，可同时允许单项与多项选择题。
- 只为用户本轮明确要求的知识范围建立单元。除非用户明确提出，不得自行增加相近方辨析、拓展方剂、其他治法或其他章节。
- 当前请求含“相关、这个、上述、刚才、前面、同主题、对应”等承接表达时，必须使用 `conversation_scope.previous_user_request` 解析所指主题。此时 `planning_context` 只能补充学习者背景，不能替换上一轮主题；例如上一轮讲解感冒、本轮说“给我一套相关试卷”，整卷范围必须是感冒。
- `candidate_limit` 应大于目标题数，为整体组卷保留去重和筛选空间。
- 不生成系统 ID，不检索或选择具体题目，不生成试卷正文、答案或解析。
- 用户未提供题量、分值或时长时写入 assumptions 或保持对应可选字段为空，不得伪造官方规范。
- 难度：只有当用户在请求或 `exam_constraints` 中明确写出数字难度（例如“难度3”“3级难度”“出5道难度4的题”）时，蓝图单元才写入 `target_difficulty`（1-5）并标记为硬约束；未明确写数字难度时，各单元保持无难度约束，不得用“简单/中等/困难”等模糊词推断数字，也不得沿用画像中任何旧难度偏好。
- 组卷按“指定难度正式题→未标注正式题→网络参考→生成补充题”的顺序降级补足题量；蓝图应把难度要求写成检索表达与选题规则的一部分（如“只选难度3的正式题；正式题不足时用未标注难度的正式题补足，并明确告知用户”）。
- `session_time_budget_minutes` 是本次学习或操作的可用时间上限，不等于正式试卷时长；只有当前请求或 `exam_constraints.duration_minutes` 明确给出作答时长时才填写 `duration_minutes`。
- 用户未明确指定总题量时，各单元 `required_question_count` 只是候选池容量范围内的建议目标，不是发布硬门槛；不得根据分钟数推导“每分钟一题”等强制规则。用户明确写出题量时才作为硬约束。
- `planning_context` 中保留当前长期规划、短期计划和当日任务的模型相关内容；不得因为精简提示词而省略计划正文或结构化长期阶段。
- 用户引用“第N阶段”时，必须以 `learning_scope` 和 `planning_context.long_term_plan` 中的当前长期规划为组卷范围，不能被画像中的其他近期目标替换。结构化阶段存在时优先采用其目标和教材；只有正文时，从长期规划正文中的阶段路径和阶段里程碑解析，不得改用近期目标猜测该阶段。
- 用户未指定题型且试卷覆盖多个知识单元时，默认同时包含客观题与至少一种简答题或案例分析题；不得把综合测试卷自动收缩为纯选择题。
- 用户请求“根据我的学习进度/掌握情况/薄弱点”组卷时，必须以 `learning_progress` 为准确定单元范围：`learning_progress.available=true` 时优先覆盖快照中出现的当前学习主题、薄弱知识点、待复习知识点和计划当前阶段，并明确告知用户该范围来自其学习进度；`learning_progress.available=false` 或 `evidence_status` 不是 `observed` 时，不得编造学习进度，只按当前请求明确写出的主题组卷，并在蓝图的 `assumptions` 中写明“未读取到可用学习进度数据，已按请求范围出卷”。

## 整体约束

当前自然语言请求代表用户本轮最终意图；当它明确给出的题量、题型、范围、时长或分值与 `exam_constraints`、用户画像等随附数据冲突时，以当前请求为准，随附数据仅补充当前请求未说明的约束。例如“全是填空题，至少25题”必须覆盖数据包中的“4题、单选与简答”配置，生成不少于25道且仅含填空题的蓝图；不得取交集、折中或沿用旧配置。

各单元的目标题数和分值应与用户明确约束一致。验收条件应覆盖候选来源、全卷去重、分值算术、题型结构、答案隔离和教学安全。
