# 贡献与协作

本文记录时珍智训的主要贡献者、协作流程与提交规范。本项目欢迎围绕中医药学习、多智能体应用及相关工程实践的代码、文档和测试贡献。

## 参与成员

主要贡献者及其工作内容如下：

| 成员 | 主要工作 |
| --- | --- |
| [Creeperw](https://github.com/Creeperw) | 多智能体后端、前后端整合、章节层级、数据持久化，以及接口和部署文档 |
| sunjingyan / [Monologue-8106](https://github.com/Monologue-8106) | 登录体验、顺序学习路径、知识空间和学习工坊的前端交互 |
| 11075 | 团队代码交接、PowerShell 启动脚本和端口配置校准 |

贡献记录以实际参与工作为依据，不仅限于 GitHub 提交统计。如有遗漏、分工描述不准确或署名调整需求，可联系项目维护者或提交文档修订。

## 问题反馈与功能建议

问题反馈通过 Issue 提交，建议包含以下信息：

- 涉及的页面或功能，以及具体操作。
- 预期结果与实际表现。
- 使用的系统、浏览器，以及 `stub` 还是 `live` 模式。
- 能复现问题的步骤，必要时附上截图或脱敏后的错误日志。

公开 Issue 不得包含模型密钥、数据库密码、Cookie 或真实用户数据。涉及隐私或安全漏洞时，应先通过非公开渠道联系项目维护者，避免披露可被利用的细节。

新功能或较大规模的结构调整应先讨论需求、范围与兼容性，再开展实现。文档修订、测试补充及小范围问题修复可直接提交 Pull Request。

## 提交代码

从最新 `main` 创建功能分支，每次提交围绕一个明确目标。Pull Request 应说明变更内容、变更原因及验证结果，并列明尚未验证的部分。

代码审查重点包括问题解决效果、既有功能兼容性和可维护性。合并时保留贡献者署名，不因提交记录整理而移除实际贡献归属。

提交代码须遵循以下约定：

- 后端代码放在 `backend/`，前端代码放在 `frontend/llm/`。接口变化时同步更新契约和 [前端接口文档](docs/frontend-api-reference.md)。
- 数据库结构变化使用新的编号迁移，不修改已经执行过的迁移文件。涉及已有数据的操作要说明备份和恢复办法。
- 不提交真实 `.env`、密钥、数据库、用户上传、运行快照或向量索引。测试尽量使用小型合成数据，并明确它不是实际学习记录。
- 用户隔离以服务端登录身份为准，不能靠前端传入的用户 ID 决定数据归属。
- 页面展示计划、掌握度和学习成果时，以后端保存的结果为准，不从模型正文中推断阶段、评分或完成状态。
- 自由文本的意图通过可靠的语义判断或明确的接口字段处理，不以简单关键词匹配代替任务判断。
- 智能体内容优先使用清晰的自然语言表达；仅对执行、校验和持久化所需字段设置明确的结构化约定。

## 提交前检查

前端改动请在 `frontend/llm` 下运行相关测试，并检查 lint 和构建：

```bash
npm run test:unit
npm run lint
npm run build
```

后端改动应补充或运行对应模块的测试，具体入口见 [后端 README](backend/competition_app/README.md)。存在测试失败时，应在 Pull Request 中区分既有问题与本次改动引入的问题。

涉及模型调用、规划生成或中断恢复时，还要在真实前端检查一次完整操作。WSL 下不要通过命令行运行 Live pytest，使用已启动运行面板的 Execute，并确认 `COMPETITION_APP_MODE=live`。不要为了测试另起一个会操作相同用户数据的应用实例。

仅涉及文字修订时，应检查 Markdown 格式、相对链接及命令的准确性，无需运行与改动无关的应用测试。

## 相关文档

- [系统架构](docs/architecture.md)
- [部署与升级](docs/deployment.md)
- [数据库运维](docs/database-operations.md)
- [前端接口参考](docs/frontend-api-reference.md)
- [学情监测与资源匹配](docs/learning-monitoring-methodology.md)
